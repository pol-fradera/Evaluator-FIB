/** @file Cjt_cursos.cc
    @brief Codi de la classe Cjt_cursos
*/

#include "Cjt_cursos.hh"

void Cjt_cursos::llegir_cursos() {
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        int m;
        cin >> m;
        Cjt_sessions c;
        for (int j = 0; j < m; ++j) {
            string s;
            cin >> s;
            afe
            
            vcurs.push(c);
}

