/** @file Cjt_usuaris.cc
    @brief Codi de la classe Cjt_usuaris
*/

#include "Cjt_usuaris.hh"
 
void Cjt_usuaris::llegir_usuaris() {
    int n;
    cin >> n;
    string p;
    for (int i = 0; i < n; ++i) {
        cin >> p;
        mu[p];
    }
}
